'use client';

import React from 'react';

interface TableLabelProps {
  tableId: string;
  className?: string;
}

export function TableLabel({ tableId, className }: TableLabelProps) {
  // Simplified table label - functionality can be added later
  return null;
}