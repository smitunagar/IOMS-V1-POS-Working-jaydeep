'use client';

import React from 'react';

interface TableNodeProps {
  tableId: string;
  className?: string;
}

export function TableNode({ tableId, className }: TableNodeProps) {
  // Simplified table node - functionality can be added later
  return null;
}