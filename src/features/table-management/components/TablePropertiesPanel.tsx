'use client';

import React from 'react';

interface TablePropertiesPanelProps {
  tableId: string;
  className?: string;
}

export function TablePropertiesPanel({ tableId, className }: TablePropertiesPanelProps) {
  // Simplified table properties panel - functionality can be added later
  return null;
}