'use client';

import React from 'react';

interface TableStatusBadgeProps {
  status: string;
  className?: string;
}

export function TableStatusBadge({ status, className }: TableStatusBadgeProps) {
  // Simplified table status badge - functionality can be added later
  return null;
}